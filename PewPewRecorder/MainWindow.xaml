<Window x:Class="PewPewRecorder.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="PewPew Recorder"
        Height="520" Width="480"
        MinHeight="480" MinWidth="420"
        ResizeMode="CanMinimize"
        Background="#1E1E2E"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#CDD6F4"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#313244"/>
            <Setter Property="Foreground" Value="#CDD6F4"/>
            <Setter Property="BorderBrush" Value="#45475A"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#313244"/>
            <Setter Property="Foreground" Value="#CDD6F4"/>
            <Setter Property="BorderBrush" Value="#45475A"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="IsReadOnly" Value="True"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#45475A"/>
            <Setter Property="Foreground" Value="#CDD6F4"/>
            <Setter Property="BorderBrush" Value="#585B70"/>
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="PewPew Recorder" FontSize="22" FontWeight="Bold" Foreground="#89B4FA"/>
            <TextBlock Text="Efficient GPU screen capture with hardware encoding" Foreground="#6C7086" Margin="0,4,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="1">
            <Grid Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Resolution"/>
                <ComboBox x:Name="ResolutionCombo" Grid.Column="1"/>
            </Grid>

            <Grid Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Frame rate"/>
                <ComboBox x:Name="FpsCombo" Grid.Column="1"/>
            </Grid>

            <Grid Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Save to"/>
                <TextBox x:Name="OutputPathBox" Grid.Column="1" Margin="0,0,8,0"/>
                <Button x:Name="BrowseButton" Grid.Column="2" Content="Browse" Click="BrowseButton_Click" Padding="10,6"/>
            </Grid>

            <Grid Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="System audio"/>
                <ComboBox x:Name="SystemAudioCombo" Grid.Column="1"/>
            </Grid>

            <Grid Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Microphone"/>
                <ComboBox x:Name="MicCombo" Grid.Column="1"/>
            </Grid>

            <Grid Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Encoder"/>
                <TextBlock x:Name="EncoderLabel" Grid.Column="1" Foreground="#A6E3A1"/>
            </Grid>
        </StackPanel>

        <StackPanel Grid.Row="2" HorizontalAlignment="Center" Margin="0,8,0,16">
            <TextBlock x:Name="TimerLabel" Text="00:00:00" FontSize="28" FontWeight="Bold"
                       Foreground="#F38BA8" HorizontalAlignment="Center" Visibility="Collapsed"/>
            <Button x:Name="RecordButton" Content="● Start Recording" Click="RecordButton_Click"
                    Width="200" Height="44" Margin="0,8,0,0"
                    Background="#A6E3A1" Foreground="#1E1E2E" FontWeight="Bold" FontSize="15"/>
        </StackPanel>

        <Border Grid.Row="3" Background="#313244" CornerRadius="6" Padding="10,8">
            <TextBlock x:Name="StatusLabel" Text="Ready" Foreground="#6C7086" TextWrapping="Wrap" FontSize="12"/>
        </Border>
    </Grid>
</Window>
